apiVersion: v1
kind: Service
metadata:
    name: "kmac-9952"
    labels:
        app: "kmac-9952"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kmac-9952"